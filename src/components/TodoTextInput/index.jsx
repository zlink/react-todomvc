import React from 'react';
import { TodoInputWrapper } from './style';

const TodoTextInput = () => (
  <TodoInputWrapper>
    <label></label>
    <input type="text" placeholder="What need to be done ?" />
  </TodoInputWrapper>
);

export default TodoTextInput;
